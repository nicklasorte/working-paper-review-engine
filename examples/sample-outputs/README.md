# Sample Outputs

Store export-ready comment matrices, run manifests, and evaluation summaries here. Align every file with the published schemas and include a short provenance note (run_id, persona set, rubric version).
