# Examples

Use this directory to store reference materials for the Working Paper Review Engine.

- Sample working paper PDFs and derived structure maps.
- Hand-authored gold comment matrices for baseline comparison.
- Comparison runs across personas and rubrics.
- Export-ready CSV/XLSX/JSON artifacts aligned to schemas.

See `examples/sample-inputs/` for input fixtures and `examples/sample-outputs/` for expected matrices and manifests.
